<h1><%= @user.name %></h1>
<hr>
<p><%= @user.email %></p>

<%= link_to "一覧へ",admin_users_path,class: 'btn btn-primary'%>

<%= link_to "編集", edit_admin_user_path(@user),class: 'btn btn-success' %>

<%= link_to '削除する', admin_users_path(@user), method: :delete, data:{ confirm: '本当に削除してよろしいですか？'}, class: 'btn btn-danger' %>
